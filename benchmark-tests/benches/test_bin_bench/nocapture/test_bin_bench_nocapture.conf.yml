groups:
  - runs:
      # FIX: test --nocapture with --load-baseline which should not print the footer
      # FIX: more tests (with setup, teardown, ...)
      - args: ["--nocapture"]
        expected:
          stdout: expected_stdout.1
